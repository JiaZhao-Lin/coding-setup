# This file is sourced by the shell to set up environment variables for ROOT.

ROOT_PREFIX=$(brew --prefix root)

PATH=$ROOT_PREFIX/bin:$PATH
DYLD_LIBRARY_PATH=$ROOT_PREFIX/lib:${DYLD_LIBRARY_PATH}
PYTHONPATH=$ROOT_PREFIX/lib/root:${PYTHONPATH}